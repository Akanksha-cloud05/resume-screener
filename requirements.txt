# Core Dependencies
streamlit==1.28.1
pandas==2.0.3
numpy==1.24.3
scikit-learn==1.3.0
PyMuPDF==1.23.8
plotly==5.15.0

# Testing
pytest==7.4.0
pytest-cov==4.1.0

# OPTIONAL: BERT Support 
# Uncomment the two lines below for semantic scoring.
# Note: Requires ~2GB free disk space. If torch DLL errors occur on Windows, 
# leave these commented; the engine will gracefully fall back to TF-IDF.
# torch>=2.0.1
# sentence-transformers>=2.2.2