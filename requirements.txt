# ============================================================================
# AI Resume Screener - Requirements
# Pinned versions ensure reproducibility across machines
# ============================================================================

# Core dependencies (required)
streamlit==1.28.1              # Web UI framework for dashboards
pandas==2.0.3                  # Data manipulation and CSV handling
numpy==1.26.0                  # Numerical computing (used by sklearn, transformers)
scikit-learn==1.3.0            # TF-IDF vectorizer, cosine similarity
PyMuPDF==1.23.8                # PDF text extraction (install as: pip install PyMuPDF)
plotly==5.15.0                 # Interactive charts and visualizations

# Testing (required for development)
pytest==9.1.1                  # Unit testing framework (9.1.1 tested)
pytest-cov==4.1.0              # Coverage reporting for tests

# ============================================================================
# OPTIONAL: BERT for Better Accuracy
# 
# Uncomment ONLY if:
#   1. You have 2+ GB disk space available (model weights)
#   2. You have 2+ GB RAM available during first run
#   3. You do NOT get "DLL not found" errors on Windows
#
# Benefits: 92% accuracy (semantic understanding)
# Cost: 30s initial load, 500MB disk space
#
# Installation:
#   pip install torch sentence-transformers
#
# If torch DLL errors on Windows:
#   - Fallback to TF-IDF mode automatically (85% accuracy, instant)
#   - Comment out the lines below
#   - Re-run: pip install -r requirements.txt
# ============================================================================
 # torch==2.0.1  
 # sentence-transformers==2.2.2

# ============================================================================
# Notes:
# - All versions are pinned (==) for reproducibility
# - TensorFlow/BERT are optional; TF-IDF works without them
# - Windows users: PyMuPDF sometimes needs Visual C++ redistributables
# - Linux/Mac: Usually installs without issues
#
# Test installation:
#   python -c "import streamlit, pandas, numpy, sklearn, fitz, plotly; print('✅ All imports OK')"
# ============================================================================